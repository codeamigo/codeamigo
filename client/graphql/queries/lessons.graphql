query Lessons(
  $status: String!
  $ownerId: Float
  $labels: String
  $template: String
) {
  lessons(
    options: {
      status: $status
      ownerId: $ownerId
      labels: $labels
      template: $template
    }
  ) {
    ...RegularLessonItem
  }
}
