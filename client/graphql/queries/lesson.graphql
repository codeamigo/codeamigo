query Lesson($id: Int!) {
  lesson(id: $id) {
    id
    title
    description
    owner {
      username
    }
    steps {
      id
      createdAt
      instructions
      codeModules {
        id
        name
        value
      }
    }
  }
}
