query Lesson($id: Int!) {
  lesson(id: $id) {
    id
    description
    label
    status
    thumbnail
    title
    owner {
      id
      username
    }
    steps {
      ...RegularStep
    }
  }
}
