fragment RegularStep on Step {
  id
  createdAt
  currentCheckpointId
  executionType
  instructions
  isCompleted
  name
  codeModules {
    ...RegularCodeModule
  }
  checkpoints {
    ...RegularCheckpoint
  }
}
