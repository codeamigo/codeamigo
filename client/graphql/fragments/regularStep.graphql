fragment RegularStep on Step {
  id
  createdAt
  currentCheckpointId
  instructions
  isCompleted
  isExecutableByBrowser
  name
  codeModules {
    ...RegularCodeModule
  }
  checkpoints {
    ...RegularCheckpoint
  }
}
