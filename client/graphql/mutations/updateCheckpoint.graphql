mutation UpdateCheckpoint(
  $id: Float!
  $description: String!
) {
  updateCheckpoint(
    id: $id
    options: { description: $description }
  ) {
    id
  }
}

mutation CompleteCheckpoint(
  $id: Float!
) {
  completeCheckpoint(
    id: $id
  ) {
    ...RegularCheckpoint
  }
}
