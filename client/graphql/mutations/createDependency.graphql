mutation CreateDependency(
  $stepId: Float!
  $package: String!
  $version: String!
) {
  createDependency(
    stepId: $stepId
    options: { package: $package, version: $version }
  ) {
    ...RegularDependency
  }
}
