mutation UpdateCodeModule(
  $id: Float!
  $name: String!
  $value: String!
  $lessonId: Float
) {
  updateCodeModule(
    id: $id
    options: { name: $name, value: $value, lessonId: $lessonId }
  ) {
    ...RegularCodeModule
  }
}
