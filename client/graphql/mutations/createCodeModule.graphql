mutation CreateCodeModule($stepId: Float!, $lessonId: Float, $name: String!, $value: String!) {
  createCodeModule(stepId: $stepId, options: { name: $name, lessonId: $lessonId, value: $value }) {
    ...RegularCodeModule
  }
}
