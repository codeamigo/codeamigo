query Step($slug: String!) {
  step(slug: $slug) {
    id
    instructions
    position
    slug
    nextSlug
    prevSlug
    start
    title
    checkpoints {
      description
      matchRegex
      isCompleted
    }
  }
}
