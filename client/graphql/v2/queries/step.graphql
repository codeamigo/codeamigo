query Step($slug: String!) {
  step(slug: $slug) {
    executionType
    id
    instructions
    nextSlug
    position
    prevSlug
    questions {
      value
    }
    slug
    start
    template
    title
  }
}
